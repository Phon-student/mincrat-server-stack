# Generated by setupmc.com on 2025-04-14T11:36:46.936Z

services:
  mc:
    image: itzg/minecraft-server:java17
    tty: true
    stdin_open: true
    ports:
      - "25566:25565"
    environment:
      EULA: "TRUE"
      TYPE: "AUTO_CURSEFORGE"
      VERSION: "1.16.5"
      CF_PAGE_URL: "https://www.curseforge.com/minecraft/modpacks/roguelike-adventures-and\
        -dungeons-2"
      CF_API_KEY: "$$2a$$10$$bA6.2mGmk606PaYspFY6juhUcvFwc78AyRCEpcGuV4wgPcpJWkEPO"
      MEMORY: "8192M"
      MAX_PLAYERS: "10"
    volumes:
      - "./data:/data"

  infrared:
      image: haveachin/infrared
      container_name: infrared
      restart: unless-stopped
      stdin_open: true
      tty: true
      ports:
        - 25565:25565/tcp  # Infrared listens on 25565
      volumes:
        - ../infrared:/proxies  # Ensure this points to your actual config directory on the host
        - /var/run/docker.sock:/var/run/docker.sock  # Optional, if Infrared needs Docker socket access
      expose:
        - "25565"
      environment:
        INFRARED_CONFIG_PATH: "/config"  # Points to where your config.yml file is inside the container
      